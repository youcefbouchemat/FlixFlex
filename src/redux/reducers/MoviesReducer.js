import {  } from "./ActionName";

const INITIAL_STATE = {
  };

  export default (state = INITIAL_STATE, action) => {
    switch( action.type ) {
        case :
            return {
            ...state,
            };
        case :
          return {
          ...state,
          };
        default:
            return state;
    }
  };